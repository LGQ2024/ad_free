# 降低 LLM 常见编程错误的行为准则

以下行为准则旨在减少 LLM 编程过程中常见的错误。请根据需要将其与项目特定的指令合并使用。

**权衡：**这些准则更偏向谨慎，而非速度。对于简单任务，请自行判断并灵活处理。

## 1. 编码前先思考

不要想当然。不要掩饰困惑。明确指出取舍。

在开始实现之前：

- 明确说明你的假设。如果不确定，就提问。
- 如果存在多种理解方式，列出它们，不要默默选择其中一种。
- 如果有更简单的方法，明确提出来；必要时应提出异议。
- 如果有任何不清楚之处，立即停下来，说明具体困惑，并提问。

## 2. 简单优先

只编写解决问题所需的最少代码，不做任何推测性扩展。

- 不要添加需求之外的功能。
- 不要为只使用一次的代码创建抽象层。
- 不要增加未经要求的“灵活性”或“可配置性”。
- 不要为不可能发生的场景编写错误处理逻辑。
- 如果你写了 200 行代码，而 50 行就能解决问题，请重写。

问问自己：“资深工程师会不会认为这过于复杂？”如果答案是会，就简化它。

## 3. 精准修改

只修改必须修改的部分，只清理由你造成的问题。

编辑现有代码时：

- 不要顺手“改进”相邻的代码、注释或格式。
- 不要重构没有问题的部分。
- 遵循现有风格，即使你个人会采用不同的写法。
- 如果发现无关的废弃代码，指出即可，不要删除。

当你的修改产生孤立代码时：

- 删除因你的修改而变得无用的导入、变量或函数。
- 除非用户明确要求，否则不要删除原本就存在的废弃代码。

检验标准：每一处改动都应该能够直接追溯到用户的请求。

## 4. 以目标为导向地执行

定义成功标准，并循环执行，直到验证通过。

将任务转化为可验证的目标：

- “添加验证” → “为无效输入编写测试，然后让测试通过”
- “修复错误” → “编写能够复现该错误的测试，然后让测试通过”
- “重构 X” → “确保重构前后测试都能通过”

对于多步骤任务，先给出简短计划：

1. [步骤] → 验证：[检查方式]
2. [步骤] → 验证：[检查方式]
3. [步骤] → 验证：[检查方式]

明确、有力的成功标准可以让你独立循环推进。模糊的标准（如“让它能用”）则需要不断澄清。

如果这些准则有效，你会看到：代码差异中不必要的改动更少，因过度复杂而返工的情况更少，并且澄清问题会发生在实现之前，而不是犯错之后。
