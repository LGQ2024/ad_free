# 隐私说明

## 应用处理什么

- 为判断是否拦截，临时读取发往虚拟 DNS 地址的查询域名。
- 在内存中最多保存最近 500 条域名、时间和拦截结果，供本机界面查看。
- 在内存中展示 PackageManager 返回的已安装应用列表。

## 写入本机什么

仅持久化以下数据：

- 用户白名单域名；
- 用户选择绕过的应用包名；
- 规则来源、版本、更新时间、条目数和 SHA-256；
- 当日与累计拦截/放行数字。

这些数据使用 Android Keystore 生成的不可导出 AES-256-GCM 密钥加密，并保存在应用 `noBackupFilesDir` 的版本化 `AtomicFile` 中。规则正文保存在应用私有目录。完整应用列表和 DNS 查询历史不写入磁盘。

## 何时联网

应用自身只允许两类外联：

1. VPN 运行期间，向当前系统网络属性提供的 DNS 地址发送 UDP/TCP 53 查询；
2. 用户主动点击更新时，访问固定的 `https://anti-ad.net/domains.txt`。

没有后台更新、启动检查、遥测、崩溃上报、广告请求、账号同步或远程配置。更新服务器会看到连接 IP 和时间，但不会收到 DNS 日志、应用列表或设备标识。

## 谁仍可能看到查询

Wi‑Fi/运营商提供的系统 DNS 仍能看到被放行的正常 DNS 查询；“净网”不会额外复制或上传这些查询。Android、设备固件和底层网络不受本项目控制。

## 备份与删除

Android 云备份和设备迁移备份均已关闭。停止 VPN 会清空内存日志；卸载应用会由 Android 删除应用私有数据和 Keystore 密钥。
