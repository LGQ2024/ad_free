# 安全说明

## 安全结论

本项目不承诺绝对不存在未知漏洞或供应链风险。它通过完整本地源码、最小权限、固定外联、依赖锁、工件 SHA-256 验证、离线漏洞扫描和正式签名验证，提供可重复检查的高可信证据。这不是独立第三方安全审计结论。

## 已实施控制

- VPN 只添加 `10.111.222.2/32` 和 `fd66:6a69:6e67::2/128` 两条 DNS 路由。
- 上游 DNS 只能来自 `ConnectivityManager` 的非 VPN 系统网络，端口固定为 53；UDP/TCP 套接字均调用 `VpnService.protect()` 并绑定对应 `Network`。
- DNS/IP 解析具有长度、标签、压缩指针、分片、并发和响应大小边界；畸形输入被拒绝。
- 规则更新 URL 为编译期常量，只接受 HTTPS 200，不跟随重定向；内容只按 UTF-8 域名文本解析。
- 远程规则受 8 MiB、300,000 行、253 字符域名、有效比例、最少 10,000 条和骤减 50% 检查；验证完成后才原子替换。
- 查询域名不写入磁盘、Logcat、异常消息或崩溃报告。
- Release `debuggable=false`、不混淆，便于反编译核对；源码和构建文件不包含秘密。

## 供应链

- 插件和直接依赖均为精确版本，仓库仅为 Google Maven 官方 CDN、Maven Central 和 Gradle Plugin Portal，并按组过滤 Google 仓库。
- `app/gradle.lockfile` 锁定所有已解析直接及传递依赖。
- `gradle/verification-metadata.xml` 对插件和依赖工件执行 SHA-256 严格验证；离线任务图已复验成功。
- 已尝试收集 PGP 公钥，但官方签名/密钥响应持续静默超时。因此当前元数据的 `verify-signatures=false`，不能声称已经完成 PGP 校验；所有组件仍必须通过 SHA-256。
- OSV-Scanner 2.4.0 可执行文件按官方发布摘要校验后，仅在构建环境运行。扫描使用 Maven 全量离线数据库，SBOM 未上传。

## 已知边界

- Android 系统、设备固件、系统 DNS、anti-AD 服务和用户签名密钥属于外部信任边界。
- 恶意或已被攻陷的系统 DNS 能观察、篡改或阻断正常 DNS；本应用不提供 DoH/DNSSEC。
- 应用绕过依赖包名；被绕过应用的 DNS 不受保护。
- Android VPN 同一时间只能有一个主要提供者，其他 VPN/私有 DNS/厂商网络栈可能产生兼容问题。
- 真机行为、耗电、网络切换、IPv6 和待机恢复仍需按手动清单验证。

## 漏洞报告

本项目是本地自用工程，没有远程接收渠道。发现问题时请保留复现步骤、Android 版本、设备型号和不含真实域名的最小测试数据；不要提交签名密钥、密码、DNS 日志或设备标识。
