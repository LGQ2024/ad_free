# 威胁模型

## 保护目标

- DNS 查询历史及其来源应用归属不持久化、不被应用上传。
- 白名单、自定义拦截域名、绕过包名、统计和规则元数据在静态存储中不可直接读取或无痕篡改。
- 远程数据不能成为代码、脚本、URL 或动态配置。
- 普通应用流量不进入本 VPN，降低流量窥探面。
- 构建依赖和最终 APK 可由用户独立核对。

## 主要威胁与控制

| 威胁 | 控制 | 剩余风险 |
|---|---|---|
| 恶意 DNS/IP 包导致越界、循环或资源耗尽 | 有界解析、压缩指针访问集合、最大消息、拒绝分片、32 并发上限、超时 | 内核/VpnService 漏洞不在应用控制内 |
| 规则服务返回恶意或异常数据 | 固定 HTTPS 主机、禁重定向、纯文本解析、大小/行数/比例/骤减检查、原子替换 | 系统 CA 或服务同时失陷仍可改变广告域名集合，但不能执行代码 |
| 查询日志与来源应用归属泄露 | 仅内存 500 条、停止清空、无 Logcat/遥测；Android 10+ 仅调用系统连接归属 API | 已解锁设备上的高权限恶意软件可读进程内存；系统可能无法返回归属或多个包共享 UID |
| 本地崩溃报告泄露 | 仅保留最近一次、应用私有 `noBackupFilesDir`、128 KiB 上限、不联网、可删除、复制时标记敏感 | 完整异常消息可能含运行数据；用户复制后，系统剪贴板及接收方进入信任边界 |
| 本地设置被复制或篡改 | Keystore AES-256-GCM、不可导出密钥、AtomicFile、禁止备份 | 已解锁且具 root/系统权限的攻击者超出边界 |
| VPN 递归或错误接管流量 | 仅虚拟 DNS 主机路由、应用自身绕过、套接字 `protect()`、绑定非 VPN 网络 | 厂商网络栈差异需真机验证 |
| 依赖投毒 | 精确版本、仓库限制、锁文件、Wrapper/工件 SHA-256、离线复验、SBOM/OSV | 当前未完成 PGP 公钥验证；上游在首次可信哈希生成前失陷属于引导风险 |
| 签名密钥泄露 | 密钥放项目外、交互式生成、密码不进源码/聊天/日志、构建后清除环境变量 | 用户主机或终端被攻陷不在应用控制内 |

## 不属于本项目的保证

- 不隐藏用户访问行为于系统 DNS、网络运营商或 Android 系统。
- 不拦截内嵌 IP、加密 DNS、第一方广告或不依赖域名的广告。
- 不提供防病毒、应用沙箱、HTTPS 解密或设备完整性证明。
